/**
 * This file was auto-generated by Fern from our API Definition.
 */

import { HotschedulesToast } from "@fern-api/toast";

/**
 * A Toast platform order is composed of one or more checks. Each check has
 * one or more menu item selections.
 *
 */
export interface Order extends HotschedulesToast.orders.ExternalReference {
    /**
     * The business date of the order. For dine-in and as soon as possible (ASAP) orders, `openedDate` should match `createdDate`. For scheduled orders, `openedDate` should match `promisedDate`. If you do not provide a value for  `openedDate` value when you `POST` a new order, the business date of the order is set to the restaurant business day that corresponds to the current date and time. The business date of an order is affected by the business date cutoff time for a restaurant, which is available from the restaurants API in the `closeoutHour` property.
     *
     */
    openedDate?: string;
    /** The most recent date that the order, or a check or menu item selection in the order, was modified. */
    modifiedDate?: string;
    /**
     * For scheduled orders, the date and time that the order is scheduled to be fulfilled. For dine-in and as soon as possible (ASAP) orders, `promisedDate` is `null`.
     *
     */
    promisedDate?: string;
    /** For internal use only. The GUID representing the ordering channel that is attributed to the order. Examples of channels include Doordash and Toast Online Ordering. */
    channelGuid?: string;
    diningOption: HotschedulesToast.orders.ExternalReference;
    /**
     * The checks for this order. Most orders have one check.
     * If the check is split, then there are multiple checks.
     *
     */
    checks: HotschedulesToast.orders.Check[];
    table?: HotschedulesToast.orders.ExternalReference;
    serviceArea?: HotschedulesToast.orders.ExternalReference;
    restaurantService?: HotschedulesToast.orders.ExternalReference;
    revenueCenter?: HotschedulesToast.orders.ExternalReference;
    /**
     * Indicates the way that the order was placed.
     * Valid values:
     * * `In Store`
     * * `Online`
     * * `Order-and-Pay-at-Table`
     * * `API`
     * * `Kiosk`
     * * `Caller Id`
     * * `Google`
     * * `Invoice`
     * * `Toast Pickup App`
     * * `Toast Local`
     * * `Branded Online Ordering`
     * * `Catering`
     * * `Catering Online Ordering`
     * * `Grubhub` (deprecated)
     * Response only.
     *
     */
    source?: string;
    /** The number of seconds between creation and payment. Response only. */
    duration?: number;
    deliveryInfo?: HotschedulesToast.orders.DeliveryInfo;
    /**
     * The amount of time that it will take to prepare the order. This value overrides the  default `deliveryPrepTime` or `takeoutPrepTime` that normally controls auto-firing for scheduled orders. You can use `requiredPrepTime` to handle atypical orders that will take more time than usual for a restaurant to prepare. Express the required preparation time in ISO-8601 duration format. Must be greater than zero and be an  increment of five minutes. For example, the value "PT15M" sets the required preparation time for the order to 15 minutes.
     *
     */
    requiredPrepTime?: string;
    /**
     * The date and time that the order is expected to be ready for pickup or to be delivered. This value is only set when the order dining option uses the `DELIVERY` or `TAKE_OUT` dining behavior. For other dining options, the value is `null`. Response only.
     *
     */
    estimatedFulfillmentDate?: string;
    /**
     * The number of restaurant guests that are associated with the
     * order. For example, for a dine-in order, this might be the
     * number of guests at a table.
     *
     */
    numberOfGuests?: number;
    /** Set to `true` if this order was voided. Response only. */
    voided?: boolean;
    /** The date on which this order was voided. Response only. */
    voidDate?: string;
    /** The business date (yyyyMMdd) on which this order was voided. Response only. */
    voidBusinessDate?: number;
    /** The most recent date on which this order received payment. If not specified when `POST`ing, it is set to the current system time. */
    paidDate?: string;
    /**
     * The most recent date on which the order payment status changed to `CLOSED`. This status is not returned for the order. The order is simply `CLOSED` when all of the checks on the order are `CLOSED`.
     *
     */
    closedDate?: string;
    /**
     * The date and time when the order was deleted.
     * The `deletedDate` value only
     * applies when the `deleted` value is `true`.
     * If `deleted` is `false`, the value
     * of `deletedDate` is the UNIX epoch, `1970-01-01T00:00:00.000+0000`.
     *
     */
    deletedDate?: string;
    /**
     * Set to `true` if this order is deleted. Response only.
     * For example, if you combine a
     * check into another order, the original order for the check is deleted.
     *
     */
    deleted?: boolean;
    /** The business date (yyyyMMdd) on which the order was fulfilled. Response only. */
    businessDate?: number;
    server?: HotschedulesToast.orders.ExternalReference;
    owner?: HotschedulesToast.orders.ExternalReference;
    /** Pricing features that this order is using. */
    pricingFeatures?: HotschedulesToast.orders.PricingFeaturesType[];
    /**
     * The current state of the order in the restaurant order
     * fulfillment process. For example, the `approvalStatus`
     * can indicate that an order is waiting for a restaurant
     * employee to approve it or that the order is in a
     * restaurant kitchen being fulfilled. Response only.
     * Valid values:
     * * `NEEDS_APPROVAL` - The order is created but will
     * not be fulfilled by the restaurant until an employee
     * approves it.
     * * `APPROVED` - The order is being fulfilled by the
     * restaurant or it was fulfilled in the past. Orders
     * remain in this state indefinitely after they are
     * fulfilled.
     * * `FUTURE` - The order is expected to be fulfilled by the
     * restaurant at a future date and time. Restaurant
     * employees will receive information about the order at the
     * date and time that it is ready to be fulfilled.
     * * `NOT_APPROVED` - Restaurant employees received
     * information about the order but did not approve it for
     * fulfillment. An order enters this state after a period of
     * time passes without a restaurant employee approving it.
     *
     */
    approvalStatus?: HotschedulesToast.orders.ApprovalStatusType;
    /**
     * Reserved for future use.
     *
     */
    guestOrderStatus?: string;
    createdDevice?: HotschedulesToast.orders.Device;
    /** The date and time that the Toast platform received the order. */
    createdDate?: string;
    /** Reserved for future use. Do not use the `initialDate` value for integration development. */
    initialDate?: number;
    lastModifiedDevice?: HotschedulesToast.orders.Device;
    curbsidePickupInfo?: HotschedulesToast.orders.CurbsidePickupInfo;
    deliveryServiceInfo?: HotschedulesToast.orders.DeliveryServiceInfo;
    marketplaceFacilitatorTaxInfo?: HotschedulesToast.orders.MarketplaceFacilitatorTaxInfo;
    /** For internal use only. */
    createdInTestMode?: boolean;
}
